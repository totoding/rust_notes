### day2 变量 基本类型


##### 字符串字面量 &str  
- 不可变引用  
- 储存在程序的二进制文件里  
- 编译时已知大小  
```rust
let s = "Hello World";
```
##### String 类型  
- 可变,可增长的字符串  
- 储存在堆上  
- URF-8 编码  
```rust
let mut s = String::new();
s.push_str("Hello");
s.push(' ');
s.push_str("World");
```