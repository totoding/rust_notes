### 栈数据 stack
- 速度快：栈分配和释放内存非常快
- LIFO原则：后进先出 (Last In, First Out)
- 固定大小：编译时必须知道数据大小
- 自动管理：变量离开作用域时自动清理  

基本类型 固定大小的数组 元组 所有字段都在栈上的结构体


### 堆 (Heap)
- 动态大小：运行时可变
- 较慢：分配和访问比栈慢
- 手动管理：需要显式分配和释放（Rust通过所有权自动管理）
- 灵活性高：可以存储大小未知的数据  

动态字符串 Vec - 动态数 Box - 智能指针，将数据放在堆上 大型结构体通常放在堆上